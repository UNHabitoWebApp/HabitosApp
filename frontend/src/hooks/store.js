import { useSelector, useDispatch } from "react-redux";

//Hook general para acceder a la store
export const useAppSelector = useSelector;
//Hook general para acceder al dispatch
export const useAppDispatch = useDispatch;  